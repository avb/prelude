; (add-to-list 'whitespace-style 'space-mark)
; (add-to-list 'whitespace-style 'tab-mark)
; (add-to-list 'whitespace-style 'newline-mark)

(setq whitespace-line-column 80) ;; limit line length
(setq whitespace-style '(face tabs empty trailing lines-tail space newline))

(setq whitespace-style '(tabs empty trailing lines-tail space newline))
